class CoffeeItem {
  double? rating;
  String? itemImage;
  String? title;
  String? subtitle;
  double? prize;

  CoffeeItem(
      {this.rating, this.itemImage, this.prize, this.title, this.subtitle});
}
